{
  "name": "crypto-lab-bb84",
  "private": true,
  "version": "1.0.0",
  "description": "Browser-based BB84 Quantum Key Distribution simulation — photon polarization, basis sifting, QBER eavesdropper detection, privacy amplification, AES-256-GCM. Physics-based security. No backends.",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "test": "node --import tsx --test src/bb84.test.ts",
    "preview": "vite preview",
    "test:a11y": "playwright test",
    "deploy": "npm run build && gh-pages -d dist"
  },
  "devDependencies": {
    "@axe-core/playwright": "^4.12.1",
    "@playwright/test": "^1.61.1",
    "gh-pages": "^6.3.0",
    "tsx": "^4.21.0",
    "typescript": "~6.0.2",
    "vite": "^8.0.4"
  }
}
